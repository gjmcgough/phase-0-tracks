// access the letters in the string
// switch the order for each letter
// print new reversed string

var str = "hello"

var newStr = ""
var strIndexLength = str.length - 1
for (var i = strIndexLength; i >= 0; i--) {
	newStr += (str[i]);
}


if (1 == 1)
	console.log(newStr)

